# Archive

Use this directory for retired prompts, skills, commands, and agent definitions before deletion.

Archive entry format:

```text
archive/YYYY-MM-DD-<id>/
  README.md       # why it was archived, replacement, owner
  original/       # exact copied source when safe to retain
```

Never archive secrets, auth files, histories, or tool caches.
